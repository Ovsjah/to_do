<% provide(:title, "New Task") %>
<% provide(:button_text, "Add task") %>

<h1>Add a task <%= "to #{@todo.title}" if @todo %></h1>

<div class='row'>
  <div class="col-md-6 col-md-offset-3">
    <%= render 'shared/form', model: @task, url: tasks_path(todo_id: @todo.id) %>
  </div>
</div>
